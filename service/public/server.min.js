import express from"express";import dotenv from"dotenv";import cors from"cors";import init from"./app/app.js";dotenv.config();let app=express(),port=process.env.PORT;init(app),app.listen(port,(p,o,r)=>{app.use(cors()),console.log("server running at port "+port)});